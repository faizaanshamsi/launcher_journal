<% @categories.each do |category| %>
  <%= link_to category.name, category %>
  <%= link_to "edit", edit_category_path(category) %>
  <%= link_to "destroy", category, method: :delete, data: { confirm: 'Are you sure?' } %><br>
<% end %>
<%= link_to "new", new_category_path %><br>

